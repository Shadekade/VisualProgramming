<Window x:Class="Lab_rab_Kirichenko_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение сортировок (Async/Task vs Threads)" Height="650" Width="650">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="3">
            <Label Content="Размер массива:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ArraySize}" Width="60" VerticalAlignment="Center" Margin="5,0"/>

            <Label Content="Потоков:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding MaxThreads}" Width="40" VerticalAlignment="Center" Margin="5,0"/>

            <Button Content="Сгенерировать" Command="{Binding GenerateArrayCommand}" Padding="10,2" Margin="10,0"/>
            <Button Content="ПРЕРВАТЬ ВСЕ" Command="{Binding AbortAllCommand}" Background="Tomato" Foreground="White" Padding="10,2"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="3">
            <CheckBox Content="Использовать ОБЩИЙ массив" 
                      IsChecked="{Binding UseSharedArray}" 
                      Margin="5" FontWeight="Bold" Foreground="DarkRed" VerticalAlignment="Center"/>

            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0" />

            <CheckBox Content="Использовать Tasks" 
                      IsChecked="{Binding UseTasks}" 
                      Margin="5" FontWeight="Bold" Foreground="DodgerBlue" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding OriginalArrayString}" Margin="5" FontStyle="Italic"/>

        <UniformGrid Grid.Row="3" Columns="4" Margin="3">
            <Button Content="Пузырек" Command="{Binding BubbleSortCommand}" Margin="2"/>
            <Button Content="Быстрая" Command="{Binding QuickSortCommand}" Margin="2"/>
            <Button Content="Вставками" Command="{Binding InsertionSortCommand}" Margin="2"/>
            <Button Content="Шейкерная" Command="{Binding ShakerSortCommand}" Margin="2"/>
        </UniformGrid>

        <ScrollViewer Grid.Row="4" Margin="0,10">
            <StackPanel>
                <GroupBox Header="Пузырьковая">
                    <StackPanel>
                        <ProgressBar Value="{Binding BubbleProgress}" Height="15" Margin="2"/>
                        <TextBlock Text="{Binding BubbleSortResult}" Margin="2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Быстрая">
                    <StackPanel>
                        <ProgressBar Value="{Binding QuickProgress}" Height="15" Margin="2"/>
                        <TextBlock Text="{Binding QuickSortResult}" Margin="2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Вставками">
                    <StackPanel>
                        <ProgressBar Value="{Binding InsertionProgress}" Height="15" Margin="2"/>
                        <TextBlock Text="{Binding InsertionSortResult}" Margin="2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Шейкерная">
                    <StackPanel>
                        <ProgressBar Value="{Binding ShakerProgress}" Height="15" Margin="2"/>
                        <TextBlock Text="{Binding ShakerSortResult}" Margin="2"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="5" Text="{Binding TotalComparisons}" FontWeight="Bold" FontSize="14" Margin="3"/>
    </Grid>
</Window>