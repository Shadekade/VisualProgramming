<Window x:Class="Lab_rab_4Kirichenko.View.WindowPerson"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудник" Height="280" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Background="{DynamicResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5" Text="ID:"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Id}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="5" Text="Фамилия:"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="5" Text="Имя:"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="5" Text="Дата рождения:"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Margin="5" Text="Должность:"/>

        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

        <DatePicker Grid.Row="3" Grid.Column="1" Margin="5" SelectedDate="{Binding Birthday, UpdateSourceTrigger=PropertyChanged}"/>

        <ComboBox x:Name="CmbRole" Grid.Row="4" Grid.Column="1" 
                  DisplayMemberPath="NameRole" 
                  SelectedValuePath="Id" 
                  SelectedValue="{Binding RoleId, UpdateSourceTrigger=PropertyChanged}" 
                  Margin="5"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Принять" Margin="5" Width="70" Click="Accept_Click"/>
            <Button Content="Отмена" Margin="5" Width="70" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>